# Versiones pinneadas usadas para generar el bundle CollectionsApp.exe.
#
# Este archivo NO es para `pip install -r` de runtime — es un freeze
# de los packages "pesados" que el bundle de PyInstaller embebe. Si
# hay que rebuildear desde otra maquina y replicar el mismo bundle,
# instalar estas versiones exactas antes de correr build_app.py.
#
# Generado con:
#   pip freeze | grep -iE "^(torch|...)" | sort
#
# Mantenido a mano cuando se actualizan deps de OCR / Qt / PyInstaller.

# Build system
pyinstaller==6.20.0
pyinstaller-hooks-contrib==2026.4

# UI
PySide6==6.11.0
PySide6_Addons==6.11.0
PySide6_Essentials==6.11.0

# OCR — deps pesadas (~580 MB combinados en disco; ~700 MB+ en bundle)
torch==2.11.0+cpu
torchvision==0.26.0+cpu
ultralytics==8.4.48
ultralytics-thop==2.0.19
easyocr==1.7.2
opencv-python==4.13.0.92
opencv-python-headless==4.13.0.92

# Procesamiento de imágenes / científico (arrastres transitivos de OCR)
numpy==2.4.4
scipy==1.17.1
scikit-image==0.26.0
pillow==12.2.0

# PDF / Excel / reportes (deps livianas pero versionables)
reportlab==4.5.0
pypdf==6.10.2
openpyxl==3.1.5

# Visualización (transitiva de ultralytics; pesa ~50 MB en el bundle)
matplotlib==3.10.9
